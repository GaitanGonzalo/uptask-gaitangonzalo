html(lang="en")
    head
        include partial/header
    body.login 
        .contenedor-formulario  
            h1= nombrePag 
            p Coloca tu nueva contraseña

            if locals.mensajes
                .alert  
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p=error 
                                
            form.caja-login(method="post")
                .campo 
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu password" value= password)
                //- .campo 
                //-     label(for="confirmar-password") Confirmar-Password:
                //-     input(type="password" name="confirmar-password" placeholder="Tu confirmar-password")
                .campo.enviar 
                    input.boton(type="submit" value="Reestablecer Password")
                
